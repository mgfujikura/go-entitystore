# go-entitystore
Cloud Datastore 操作用パッケージ


## テストについて

テスト時には プロジェクトルートにサービスアカウントのキーファイルを
service-account-key.json という名前で配置すること。

テストに使用する Datastore にはデフォルトデータベースと test-database が必要。
それぞれに接続確認用のエンティティを作成しておく必要がある。
（エンティティの内容についてはテストを参照）

テストでインデックスが必要になるため、./index.yaml を使ってインデックスを設定しておくこと
